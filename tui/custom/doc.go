// Package custom provides the OC-specific chat UI, built on top of the
// reusable tui/common primitives. It contains the main UI orchestrator,
// chat message renderer, status bar, and 3-panel layout computation.
package custom
